# FlowSlice AI 0.1.2 🚀

## 🆕 Что нового

- **Настройки фото на вкладке «Общие»:** сторона сжатия в пикселях (по умолчанию 1024, значение 0 —
  отправить оригинал без сжатия), качество JPEG (по умолчанию 85 %) и лимит размера файла
  (по умолчанию 16 МБ). Фото с камеры для калибровки (например, Pressure Advance) теперь можно
  прикреплять без ручного сжатия — достаточно поднять сторону сжатия или отключить его вовсе.
- **Подняты серверные лимиты изображений:** одно изображение — до 12 МБ base64 (было 6 МБ),
  суммарно на запрос — до 32 МБ base64 (было 20 МБ). Это запас под фото без клиентского сжатия.

---

## 📲 Как установить?

**Способ 1. Как плагин из каталога OrcaCloud**

1. Откройте [страницу FlowSlice AI в OrcaCloud](https://cloud.orcaslicer.com/p/3dfa96ca82dd).
2. Оформите подписку и установите плагин.
3. Перезапустите Orca Slicer — вкладка **FlowSlice AI** появится в главном окне.

**Способ 2. Локальная установка `.whl`**

1. Скачайте whl файл внизу этой страницы.
2. В Orca Slicer откройте **Плагины → Установить локальный плагин** и выберите скачанный файл.
3. Перезапустите Orca Slicer — вкладка **FlowSlice AI** появится в главном окне.

---

**Разработчик:** @FlowHack
**Build:** `v0.1.2`